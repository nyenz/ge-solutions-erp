# PATH: erp-backend/src/main/resources/application.properties

# VITAL FIX: '${PORT:10000}' means "use the PORT variable Render gives us,
# but if it's missing for any reason, default to 10000."
# The old version '${PORT}' with no default would crash the app on startup.
server.port=${PORT:10000}

spring.application.name=ge-solutions-erp

# ── NEON CLOUD VAULT ──
# These are the connection details for your PostgreSQL cloud database.
spring.datasource.url=jdbc:postgresql://ep-wispy-cell-an2afrm4.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_ws7xVYrW5GLu

# ── ENGINE SETTINGS ──
# 'update' means: on startup, add any new database columns automatically.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# ── SECURITY ──
ge.solutions.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
ge.solutions.jwt.expiration=86400000

# ── PERFORMANCE TUNING FOR FREE TIER ──
# Limits the database connection pool size to reduce memory usage.
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
# CLOUDINARY STORAGE
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}