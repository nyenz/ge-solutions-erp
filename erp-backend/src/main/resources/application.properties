# PATH: erp-backend/src/main/resources/application.properties

# ── 1. HARDWARE ──
server.port=8080
spring.application.name=ge-solutions-erp

# ── 2. THE CLOUD VAULT (NEON TECH SYNC) ──
# VITAL: This URL is forged from your wispy-cell host
spring.datasource.url=jdbc:postgresql://ep-wispy-cell-an2afrm4.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require

# VITAL: Matching the credentials from your screenshot
spring.datasource.username=neondb_owner
spring.datasource.password=npg_ws7xVYrW5GLu

spring.datasource.driver-class-name=org.postgresql.Driver

# ── 3. JPA ENGINE (CLOUD AUTO-BUILD) ──
# This tells the system to build the tables in the cloud if they are missing
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── 4. SECURITY (KEEP YOUR SAME SECRET) ──
# This ensures existing tokens stay valid
ge.solutions.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
ge.solutions.jwt.expiration=86400000

# ── 5. UPLOAD PIPE ──
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ── 6. LOGGING (DIAGNOSTIC MODE) ──
# We set this to INFO so you can see the connection succeed in the logs
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=ERROR